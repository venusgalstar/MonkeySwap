import { ThemeUIStyleObject } from 'theme-ui'

const styles: Record<'warningMessageContainer', ThemeUIStyleObject> = {
  warningMessageContainer: {
    flexDirection: 'column',
    background: 'error',
    borderRadius: '10px',
    margin: '15px 0px',
    padding: '15px',
    alignItems: 'center',
    justifyContent: 'center',
  },
}

export default styles
