import { ThemeUIStyleObject } from 'theme-ui'

export const styles: Record<'mainContainer', ThemeUIStyleObject> = {
  mainContainer: {
    position: 'relative',
    top: '30px',
    width: '100%',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItem: 'center',
    marginBottom: '80px',
  },
}
